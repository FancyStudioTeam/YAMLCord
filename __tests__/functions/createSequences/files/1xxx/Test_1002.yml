sequences:
  - if: "[message.content] eq '!admin'"
    then:
      - if: "[message.user_id] eq '945029082314338407'"
        then:
          - create_message:
              content: "You are an admin!"
          - create_reaction: "🎉"
        else:
          - create_message:
              content: "You are not an admin!"
          - create_reaction: "😢"
